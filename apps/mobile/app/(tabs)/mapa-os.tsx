import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function MapaOsScreen() {
  return (
    <PlaceholderScreen
      description="Projeções do Mapa-OS (operacional, agora/próximo/depois, status terminal, prisma 7d), como em /mapa-os no web."
      title="Mapa-OS"
    />
  );
}
