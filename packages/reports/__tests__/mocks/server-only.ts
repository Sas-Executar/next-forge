// Vitest alias target for `server-only` (see vitest.config.mts). Same
// rationale as packages/application/__tests__/mocks/server-only.ts:
// builder.ts transitively imports it via @repo/database.
export {};
